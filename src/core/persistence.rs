//! Persistence utilities — statefile read/write helpers.
//!
//! This module is a placeholder. The implementation currently lives in
//! `core::adaptation` (statefile types and `make_statefile`). A dedicated
//! `persistence` module will be introduced in a later milestone when
//! statefile logic is separated from the YAML adaptation pipeline.
